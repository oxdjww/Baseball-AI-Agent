# Daily Log — 2026-03-15

## 작업 요약
v2 Kbaseball 런칭일. docker-compose 환경 안정화 → 테스트 보강 → 네이밍 전환 → 기능 토글 →
관제 시스템 구축 → 다수 버그픽스 → 경기 종료 알림 고도화까지 전방위 작업.

## 🎯 주요 변경
- **Infra** (`4cebf9f`): docker-compose 로컬 개발 환경 구성 + 변수 보간 오타 수정
- **Fix** (`1b677fc`): 관리자 페이지 404 수정, UX 개선(컬러 뱃지·팀명 한글), `rerun.sh` 추가
- **Test** (`ae15369`): 회원가입·역전 탐지 로직 유닛 테스트 (커버리지 100% 목표)
- **Test** (`c89a081`): 기상청 API 연동 및 우천취소 알림 로직 통합 테스트
- **Refactor** (`b747679`): 운영 안정성 개선 — `@Retryable`, 병렬 스케줄링, logback 구조화
- **Refactor** (`6e6c4f8`): 프로젝트 네이밍 baa → **Kbaseball** (78파일 일괄), `FeatureToggleService` + `SystemSetting` 추가
- **Feat** (`33f80bb`): AOP + Spring Event 기반 장애 관제 시스템 — `MonitoringAspect` → `MonitoringEventListener` → Telegram
- **Fix** (`6449477`): logback stdout appender 이중 등록으로 로그 2배 출력 수정
- **Fix** (`fb54096`): `@EnableBatchProcessing` 제거로 Batch 스키마 자동 생성 버그 수정
- **Fix** (`413ff2d`): 관리자 회원 화면 `supportTeam` null NPE 수정
- **Fix** (`9f4f01d`): 실시간 역전 감지 크론 시작 시각 13:00으로 조정
- **Feat** (`d6d8399`): 경기 종료 알림 고도화 — `NaverStandingsClient`(KBO 순위), `GameEndNotificationBuilder`, 다음 경기 예고

## 🛠️ 해결한 문제 (오전)
- docker-compose `TELEGRAM_ADMIN_ID` 파싱 실패 → 변수 보간 닫는 `}` 누락 수정
- 관리자 페이지 `AdminMemberController` 템플릿 경로 오류 → 경로 및 formAction 모델 수정
- logback stdout 2중 등록으로 모든 로그가 2배 출력 → 설정 중복 제거
- `@EnableBatchProcessing`이 DataSource 스키마 자동 생성 유발 → 어노테이션 제거
- 오전 크론 불필요 실행 → 경기 시작 시각 이전인 13:00으로 크론 조정
- Naver API 응답 키 오독(`teams` → `seasonTeamStats`, `rank` → `ranking` 등) → `NaverStandingsClient` 파싱 전면 수정
- 순위 조회 실패 시 무음 생략 → `[순위 정보 조회 실패]` fallback 표시 추가

---

## 🎯 주요 변경 (오후)

- **Docs** (`f792502`): README 리뉴얼 및 데일리 로그 시스템 구축
- **DevOps** (`5730208`): Vultr VPS 배포 파이프라인 구축 — `docker-compose.prod.yml`, `Dockerfile` 멀티스테이지 빌드, GitHub Actions CI/CD, `scripts/server-setup.sh`
- **Fix** (`4f40ecf`): Spring Boot 3 Redis 설정 경로 오류 수정 (`spring.redis` → `spring.data.redis`), Telegram 링크 토큰 TTL 30분으로 연장
- **Fix** (`bb7d817`): 텔레그램 웹훅 URL 자동 등록 — `TelegramWebhookRegistrar` 신규 (`ApplicationReadyEvent` 기반), `TelegramProperties.webhookUrl` 추가, `TELEGRAM_WEBHOOK_URL` 환경변수화
- **Hotfix** (`e7cd491`): `SERVER_SERVLET_CONTEXT_PATH` 복구 — nginx가 prefix를 strip하지 않아 앱에서도 `/services/kbaseball`을 context-path로 받아야 함을 확인
- **Fix** (`4d3d530`): context-path 적용 이후 발생한 버그 4건 수정
  - 관리자 공지 발송 `fetch()` URL 하드코딩 → `th:inline="javascript"` + `@{...}` 방식으로 교체
  - 회원 수정 폼 `action` 하드코딩 → `th:action="@{${formAction}}"` 교체
  - `signup_success.html` 불필요한 닫는 태그 제거
  - 테스트 `application.yml` Redis 경로 수정 (`spring.redis` → `spring.data.redis`)
- **Feat** (`7927af5`): 무작위 회원가입 봇 공격 방어 3층 구조 적용
  - nginx `limit_req_zone` (5r/min) 레이트 리밋
  - 30분 주기 미연동 회원 purge 스케줄러
  - Honeypot hidden field (`name="website"`)
- **Feat** (`0f81e62`): **Lazy Registration 전면 개편** — 폼 제출 시 DB INSERT 없이 `pending:signup:{token}`(TTL 15분)으로 Redis에만 저장, `/start {token}` 수신 시점에 `telegramId` 포함한 레코드 INSERT. purge 스케줄러·`TelegramLinkController`·`deleteOldPending` 쿼리 전부 제거
- **Docs** (`a138aec`): README 전문성 개선 — 캐치프레이즈·로그 가이드 제거, Tech Stack 섹션 추가, 헤더 영문화
- **Docs** (`d5e5491`): README 다이어그램 전면 재작성 — Mermaid 기반 System Architecture + 5개 Sequence Diagram (Lazy Registration, 우천 알림, 역전 알림, AI 분석, 관제 시스템), Redis Key Strategy 표 추가

## 🛠️ 해결한 문제 (오후)
- 배포 후 nginx가 `/services/kbaseball` prefix를 그대로 upstream으로 전달함을 확인 → `SERVER_SERVLET_CONTEXT_PATH` 제거했다가 복구, 전체 404 장애 빠르게 롤백
- context-path 추가 이후 관리자 공지 발송 API, 회원 수정 폼 등 하드코딩 URL이 모두 깨지는 문제 일괄 수정
- 봇 계정 1,000명 이상 삽입 공격 경험 → Lazy Registration으로 DB 오염 원천 차단 (Redis TTL 만료 시 자동 소멸)
